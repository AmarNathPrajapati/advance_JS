/**
 * javascript is loosly typed languae
 * means there is no need to define type variable
 * jab tak value defined nahi hota tab tak value undefined hoti hai (usase upar wali line tak)
 * aur jisaki value assign hee nahi hoti; use not defined dwara represent hoti hai.
 */
let a;
a = "amarnath"
console.log(a);
console.log(b);